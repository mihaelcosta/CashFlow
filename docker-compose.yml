services:
  api:
    build: .
    ports:
      - "5013:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Development
    volumes:
      - cashflow-data:/data

  web:
    build: ./web
    ports:
      - "5173:80"
    depends_on:
      - api

volumes:
  cashflow-data:
